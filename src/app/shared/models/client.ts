export interface IClient {
    id: string,
    name: string,
    address: string,
    phone: string,
    quota: number,
    createdAt: Date,
    channel: string,
    sellerId: string,
    cityId: string,
    dadId: string
}