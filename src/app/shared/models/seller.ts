export interface ISeller {
    sellerId: string,
    sellerName: string,
    subtotal: number,
    comission: number,
    year: number,
    month: number
}

export interface ISellerReport {
    sellerId: string,
    sellerName: string,
    subtotal: number,
    comission: number,
    year: number,
    month: number
}